import styled from "styled-components";

export function Dashboard() {
  return (
    <Container>
      <h1>Dashboard</h1>
    </Container>
  );
}

const Container = styled.div`
  padding: 1.75rem;
  height: 100vh;
`;
